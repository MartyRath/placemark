import { assert } from "chai";
import { assertSubset } from "../test-utils.js";
import { playtimeService } from "./playtime-service.js";
import { maggie, testProvinces, testUserTrees } from "../fixtures.js";

suite("Tree API tests", () => {
  const user = null;
  const province = null;

  setup(async () => {
  });

  teardown(async () => {});

  test("create user tree", async () => {
  });

  test("create Multiple user trees", async () => {
  });

  test("Delete user Tree", async () => {
  });

  test("test denormalised province", async () => {
  });
});